@model IEnumerable<MusicBoxProj.Models.PlayList>

@{
    ViewData["Title"] = "Index";
}

<h1>Playlists</h1>

<p>
    <a class="btn btn-success" asp-action="Create">Create New</a>
</p>
<div>
    <nav class="navbar">
        <div class="container-fluid">
            <form asp-controller="PlayLists" asp-action="Index" method="get">
                Search: <input type="text" name="searchString" />
                <input type="submit" value="Filter" />
            </form>
        </div>
    </nav>
</div>
<table class="table table-striped table-hover">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PlayListName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PlayListName)
            </td>
            <td>
                    <a class="btn btn-warning" asp-action="Edit" asp-route-id="@item.PlayListId">Edit</a> |
                    <a class="btn btn-info" asp-action="Details" asp-route-id="@item.PlayListId">Details</a> |
                    <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.PlayListId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
